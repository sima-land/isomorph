{
  "name": "sima-land-isomorph",
  "version": "1.0.0",
  "description": "Server side rendering for Header service",
  "main": "index.js",
  "repository": "ssh://git@gitlab.sima-land.ru:7888/SL/isomorph.git",
  "author": "www.sima-land.ru team",
  "license": "MIT",
  "private": true,
  "publishConfig": {
    "registry": "https://gitlab.sima-land.ru/api/v4/projects/676/packages/npm/"
  },
  "scripts": {
    "dev": "cd ./examples/hello-world/ && yarn && yarn dev",
    "build": "gulp build",
    "publication": "yarn login && yarn np",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "del": "^4.1.1",
    "gulp": "^4.0.2",
    "np": "^5.0.2"
  }
}
